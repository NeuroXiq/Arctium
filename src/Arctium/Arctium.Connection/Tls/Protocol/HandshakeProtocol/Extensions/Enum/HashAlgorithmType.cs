namespace Arctium.Connection.Tls.Protocol.HandshakeProtocol.Extensions.Enum
{
    public enum HashAlgorithmType
    {
        NULL,
        MD5,
        SHA1,
        SHA224,
        SHA256,
        SHA384,
        SHA512
    }
}
