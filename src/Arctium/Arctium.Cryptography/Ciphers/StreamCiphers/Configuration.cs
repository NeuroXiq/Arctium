namespace Arctium.Cryptography.Ciphers.StreamCiphers
{
    public static class Configuration
    {
        /// <summary>
        /// Indicates size of the stream cipher buffer where
        /// data are stored before encryption
        /// </summary>
        public const int StreamBufferSize = 4 * 1024;
    }
}
